{
  "firestore": [
    {
      "database": "preamble",
      "rules": "firebase-firestore-rules.rules"
    }
  ],
  "storage": {
    "rules": "storage.rules"
  },
  "functions": {
    "source": "functions",
    "runtime": "nodejs20"
  }
}
